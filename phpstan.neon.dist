includes:
    - vendor/larastan/larastan/extension.neon
    # Pest-aware static analysis (understands it()/expect()/$this).
    - vendor/pestphp/pest-plugin-phpstan/extension.neon
    # Understands Mockery's dynamic API (shouldReceive(), etc.).
    - vendor/phpstan/phpstan-mockery/extension.neon

parameters:
    configDirectories:
    - config

    databaseMigrationsPath:
        - database/migrations
        - vendor/seatplus/auth/database/migrations
        - vendor/seatplus/eveapi/database/migrations
    level: 5
    paths:
        - src
        - config
        - database
        - tests
    tmpDir: build/phpstan
    checkOctaneCompatibility: true
    checkModelProperties: true

    # The only exceptions left, and they are framework typing limitations rather than ours.
    #
    # 1. filterBy*Ids are eveapi query scopes, declared `protected #[Scope]` — the shape
    # Larastan requires (seatplus/eveapi#723). Larastan resolves a scope from the model
    # behind `Builder<TModel>`, but these scope classes run against *any* model carrying
    # the watch-list marker interface, so the concrete model is unknowable here.
    #
    # Verified unresolvable, not assumed: typing the builder as
    # `Builder<Model&TypeWatchListInterface>` does not resolve them, and neither does
    # adding `@method scopeFilterBy*Ids` tags to the marker interface.
    #
    # 2. Eager-load callbacks that name the relation they are handed. Laravel documents
    # them as `with(['rel' => fn (HasMany $q) => ...])` and passes the concrete relation
    # at runtime, but types `with()`'s callback as `Closure(Relation)` — which makes
    # every concrete hint contravariantly invalid. Widening each hint to `Relation`
    # throws away the type that documents the callback, and for `morphTo` it does not
    # even work, since `morphWith()` is absent from `Relation`; the remaining option is
    # an `instanceof` wrapper, a second way to write an eager load that buys nothing at
    # runtime. Scoped to exactly that shape: a `with()` relations array holding a
    # `Closure(<a concrete relation class>)`.
    ignoreErrors:
        -
            message: "#^Call to an undefined method Illuminate\\\\Database\\\\Eloquent\\\\Builder.*::filterBy(System|Region)Ids\\(\\)\\.$#"
            path: src/Services/Query/LocationWatchListScope.php
        -
            message: "#^Call to an undefined method Illuminate\\\\Database\\\\Eloquent\\\\Builder.*::filterBy(Type|Group|Category)Ids\\(\\)\\.$#"
            path: src/Services/Query/TypeWatchListScope.php
        -
            identifier: argument.type
            message: '#^Parameter \#1 \$relations of method Illuminate\\Database\\Eloquent\\Builder.*::with\(\) expects .*, array\{.*Closure\(Illuminate\\Database\\Eloquent\\Relations\\[A-Za-z]+\).*\} given\.$#'
